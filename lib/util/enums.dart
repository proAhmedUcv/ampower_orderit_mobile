enum AttachmentsFilter { all, files, links }

enum ConnectivityStatus {
  wifi,
  cellular,
  offline,
}

enum ViewState {
  idle,
  busy,
}

enum Menu {
  lead,
  salesOrder,
  quotation,
  salesInvoice,

  //Multiple Drafts
  saveToDraft,
  openDrafts,
  logout,
}

enum ViewTypes {
  listView,
  gridView,
  tableView,
  catalogueView,
}
